struct Counter {
    value: int,
}

extend Counter {
    proc init() {
        return Counter{ value: 0 };
    }

    proc increment() {
        self.value = self.value + 1;
    }

    proc get() {
        self.value
    }
}
