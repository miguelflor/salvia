struct Counter {
    value: int,
}

extend Counter {
    proc init() Counter {
        Counter{ value: 0 }
    }

    proc increment() {
        self.value = self.value + 1;
    }

    proc get() int {
        self.value
    }
}
